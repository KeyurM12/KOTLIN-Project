<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mykotlin1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="ATandT"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyKotlin1">
        <activity android:name=".NoteTakingScreen2Activity"></activity>
        <activity
            android:name=".NotesTakingActivity"
            android:label="@string/title_activity_notes_taking"
            android:theme="@style/Theme.MyKotlin1.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>